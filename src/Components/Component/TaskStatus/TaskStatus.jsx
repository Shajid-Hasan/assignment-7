// import React, { use } from 'react';

const TaskStatus = () => {


    return (
        <div>
            {
                // customerTask.map(task => )
            }
        </div>
    );
};

export default TaskStatus;